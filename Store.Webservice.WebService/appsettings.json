/* Application Settings Configuration file
 * To run this solution locally, use "User Secrets" to configure the variables.
 */
{
  "SwaggerFilePath": "/swagger/v1/swagger.json",
  "ConnectionStrings": {
    "StoreDatabase": "__Database__"
    // Example: Server=localhost;Database=Store;Trusted_Connection=True;ConnectRetryCount=0
  }
}